# AI Proctor — Requirements
# Production-ready exam proctoring system

# ── Core CV & ML ──────────────────────────────────────────────────
opencv-python>=4.8.0
ultralytics>=8.0.0
numpy>=1.24.0

# ── Audio ─────────────────────────────────────────────────────────
sounddevice>=0.4.6
librosa>=0.10.0
scipy>=1.10.0

# ── API ───────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
websockets>=12.0

# ── HTTP (for event forwarding) ───────────────────────────────────
requests>=2.31.0

# ── Optional: GPU support ────────────────────────────────────────
# Uncomment for CUDA-accelerated YOLO inference:
# torch>=2.1.0
# torchvision>=0.16.0
